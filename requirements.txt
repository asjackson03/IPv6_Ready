python-nmap==0.7.1
pysnmp==4.4.12
pandas==2.2.0
python-dotenv==1.0.0
colorama==0.4.6
tqdm==4.66.1
tabulate==0.9.0
scikit-learn==1.4.0
numpy==1.26.4
joblib==1.3.2
fastapi==0.110.0
uvicorn==0.27.0
ollama==0.1.7
# httpx queda fijado a <0.26 por compatibilidad con ollama 0.1.7
# (también lo usa el TestClient de FastAPI en las pruebas del Módulo 2).
httpx==0.25.2
pytest==7.4.4
# Bloque 1 — Base de datos (SQLite + ORM).
sqlalchemy>=2.0,<2.1
# Bloque 3 — Portal web (Streamlit + gráficas Plotly).
# Streamlit se fija a <1.50 a propósito: las versiones 1.50+ migraron su
# servidor de tornado a starlette>=0.40, lo que choca con fastapi==0.110
# (Módulo 2, requiere starlette<0.37) y arrastraría httpx fuera del pin
# <0.26 que necesita ollama 0.1.7. La rama 1.41.x usa tornado y convive sin
# conflicto con el resto del stack.
streamlit>=1.30,<1.50
plotly>=5.18,<6.0
